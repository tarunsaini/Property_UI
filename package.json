{
  "name": "web-sass",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "scssCompile": "sass --watch --update --no-source-map --style=compressed assets/scss:assets/css"

  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    
    "sass": "^1.85.1"
  }
}
